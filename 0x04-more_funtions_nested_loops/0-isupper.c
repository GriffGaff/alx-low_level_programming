#include "main.h"
/**
 * _isupper - function that checks for uppercase characters
 *
 * @c: characters to be cheecked
 * Return: 1 , if character is upper.0 if lower
 */
int _isupper(int c)
{
	if (c >= 'A' &&  c <= 'Z')
		return (1);
	else
		return ('0');
}
