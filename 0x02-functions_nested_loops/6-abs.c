#include "main.h"
/**
 * _abs - function that computes the absolute value of integers
 * @n: integer
 * Return: returns absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else 
		return (0);
}
